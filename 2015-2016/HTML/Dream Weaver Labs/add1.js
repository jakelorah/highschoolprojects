function add1(i,ii)
 {
  if(isNaN(i) II isNaN(ii))
   {
    alert("Must be numeric!!");
   }
 else
   {
      var sum = 0;
      sum = (parseInt(i) + parseInt(ii));
      document.write("The sum="+sum);
   }
 }
function add1(i, ii)
 {
  if(isNaN(i) II isNaN(ii))
   {
    alert ("Must be numeric!!");
   }
 else
   {
      var sum = 0;
      sum = (parseFloat(i) + parseFloat(ii));
      document.write("The sum = " + sum);
   }
 }

(Jake Lorah)
